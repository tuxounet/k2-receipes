apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "plop.fullname" . }}-test-connection"
  labels:
    {{- include "plop.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "plop.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
